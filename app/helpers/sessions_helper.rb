helpers do
  def current_user
    if session[:user_id]
		User.find(session[:user_id])
	else
		nil
	end
  end

  def login(user)
    session[:user_id] = user.id
  end

  def logout
	session[:user_id] = nil
  end
end

helpers do
  def current_user
    if session[:user_id]
		User.find(session[:user_id])
	else
		nil
	end
  end

  def login(user)
    session[:user_id] = user.id
  end

  def logout
	session[:user_id] = nil
  end
end

helpers do
  def current_user
    if session[:user_id]
		User.find(session[:user_id])
	else
		nil
	end
  end

  def login(user)
    session[:user_id] = user.id
  end

  def logout
	session[:user_id] = nil
  end
end

helpers do
  def current_user
    if session[:user_id]
		User.find(session[:user_id])
	else
		nil
	end
  end

  def login(user)
    session[:user_id] = user.id
  end

  def logout
	session[:user_id] = nil
  end
end

helpers do
  def current_user
    if session[:user_id]
		User.find(session[:user_id])
	else
		nil
	end
  end

  def login(user)
    session[:user_id] = user.id
  end

  def logout
	session[:user_id] = nil
  end
end

helpers do
  def current_user
    if session[:user_id]
		User.find(session[:user_id])
	else
		nil
	end
  end

  def login(user)
    session[:user_id] = user.id
  end

  def logout
	session[:user_id] = nil
  end
end

helpers do
  def current_user
    if session[:user_id]
		User.find(session[:user_id])
	else
		nil
	end
  end

  def login(user)
    session[:user_id] = user.id
  end

  def logout
	session[:user_id] = nil
  end
end

helpers do
  def current_user
    if session[:user_id]
		User.find(session[:user_id])
	else
		nil
	end
  end

  def login(user)
    session[:user_id] = user.id
  end

  def logout
	session[:user_id] = nil
  end
end

helpers do
  def current_user
    if session[:user_id]
		User.find(session[:user_id])
	else
		nil
	end
  end

  def login(user)
    session[:user_id] = user.id
  end

  def logout
	session[:user_id] = nil
  end
end

helpers do
  def current_user
    if session[:user_id]
		User.find(session[:user_id])
	else
		nil
	end
  end

  def login(user)
    session[:user_id] = user.id
  end

  def logout
	session[:user_id] = nil
  end
end

helpers do
  def current_user
    if session[:user_id]
		User.find(session[:user_id])
	else
		nil
	end
  end

  def login(user)
    session[:user_id] = user.id
  end

  def logout
	session[:user_id] = nil
  end
end
