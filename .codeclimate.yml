version: "2"
checks:
  config:
    enabled: true
    issue_override:
      severity: info
complex-logic:
  config:
    enabled: true
    issue_override:
      severity: info
file-lines:
  config:
    enabled: true
    issue_override:
      severity: info
method-complexity:
  config:
    enabled: true
    issue_override:
      severity: info
method-count:
  config:
    enabled: true
    issue_override:
      severity: info
method-lines:
  config:
    enabled: true
    issue_override:
      severity: info
nested-control-flow:
  config:
    enabled: true
    issue_override:
      severity: info
return-statements:
  config:
    enabled: true
    issue_override:
      severity: info
similar-code:
  config:
    enabled: true
    issue_override:
      severity: info
identical-code:
  config:
    enabled: true
    issue_override:
      severity: info
similar-code:
  config:
    enabled: true
    severity: info
identical-code:
  config:
    enabled: true
    issue_override:
      severity: info
